import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RouteLog app smoke test', (WidgetTester tester) async {
    // Firebase 초기화가 필요하므로 기본 테스트는 스킵
    // TODO: Firebase Mock을 사용한 통합 테스트 추가 필요
  });
}
